FROM node:10

RUN apt-get update -y
RUN apt-get upgrade -y

WORKDIR /usr/src/app

COPY package.json ./

RUN npm --development --quiet install

COPY models models
COPY lib lib
COPY test test

ENTRYPOINT [ "npm" ]

CMD ["test"]
